import type React from 'react';
import { Navbar } from '../common/Navbar';

/**
 * Dashboard for placing widgets
 */
export const Dashboard: React.FC = () => {
	return (
		<div className='w-full h-full'>
			<Navbar />
		</div>
	);
};
